# bangdream-na

BanG Dream! North America fan community website.

## Stack

React 19 + Vite 8 + react-router-dom 7 + Vitest 3. Deploys to Vercel.

## Commands

```bash
npm install
npm run dev              # local dev server
npm run build            # production build to dist/
npm run preview          # preview built dist locally
npm run lint             # eslint
npm test                 # vitest run (single pass)
npm run test:watch       # vitest watch mode
npm run test:coverage    # coverage report (gate: 80%)
```

## Structure

See `docs/architecture.md` and `docs/design.md` for the Phase 1 spec.
